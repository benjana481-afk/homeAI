# Copy to .env.local for local development — leave empty (vite proxy handles it)
# VITE_API_URL=

# Production (Vercel) — set in Vercel dashboard under Environment Variables
# VITE_API_URL=https://your-app.railway.app
